// @flow

function EventFactory(eventName: string, id: string, streamId: string,  eventPayload: { [string]: any }) {
 // pass common event structure (ID, timestamp, version)
 // validate payload
 // generate standardized json object
}

export default EventFactory;
